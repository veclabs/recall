# Cargo.toml (workspace root)
[workspace]
members = [
    "crates/solvec-core",
    "crates/solvec-wasm",
]

[workspace.dependencies]
rand = "0.8"
serde = {version = "1.0",features = ["derive"] }
serde_json = "1.0"
criterion = "0.5"